{
  "name": "seedfi",
  "private": true,
  "version": "1.0.0",
  "description": "SeedFi — infraestructura de financiamiento programable para proyectos inmobiliarios",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @s2d/shared && npm run build -w @s2d/zk && cd backend && npm run build && cd ../frontend && npm run build",
    "dev": "cd backend && npm run dev & cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "chain": "anvil --chain-id 31337 --block-time 2",
    "contracts:build": "cd contracts && forge build",
    "contracts:test": "cd contracts && forge test -vv",
    "contracts:deploy:local": "bash scripts/deploy-local.sh",
    "contracts:deploy:hashkey": "cd contracts && bash script/deploy-hashkey.sh",
    "fund:demo": "cd backend && npm run fund",
    "circuit:test": "cd circuits/eligibility && nargo test",
    "circuit:build": "bash scripts/build-circuit.sh",
    "seed": "cd backend && npm run seed --",
    "e2e": "cd backend && npm run e2e --",
    "test": "npm run test -w @s2d/zk && cd backend && npm run test",
    "check:conn": "node backend/scripts/connectivity-check.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
